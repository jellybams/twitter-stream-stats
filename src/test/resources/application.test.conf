include "application.conf"

app {
  twitter {
    auth {
      consumerKey = "NOT NEEDED FOR TESTS"
      consumerSecret = "NOT NEEDED FOR TESTS"
      accessToken = "NOT NEEDED FOR TESTS"
      accessTokenSecret = "NOT NEEDED FOR TESTS"
    }
  }
}